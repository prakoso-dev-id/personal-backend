server:
  port: "8080"
  mode: "debug"
  base_url: "http://localhost:8080"

database:
  host: "localhost"
  port: "5432"
  user: "postgres"
  password: "password"
  dbname: "personal_db"
  sslmode: "disable"

jwt:
  secret: "change_this_secret_in_production"
  expiration: 24
